# cpp_primer
** 存放c++ primer第五版的课后习题中的程序实现 **  
> 在Ubuntu16.04中进行编译检验:  
> 如: 编译test1_1.cpp,通过执行指令:`g++ -o test1-1 test1-1.cpp`  
     在终端中运行可执行文件(文件在当前路径下)进行检验,通过运行指令: `./test1_1`  
     在编译带有`auto`等,C++11特有的实现时,编译要加上`-std=c++11`,即编译指令变为:`g++ -o test1-1 test1-1.cpp -std=c++11`  
